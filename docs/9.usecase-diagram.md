# 🎯 Use Case Diagram

```mermaid
graph TD

%% ACTORS
Guest((Guest))
User((User))
Manager((Manager))
Admin((Admin))

%% GUEST
Guest --> ViewProducts[Xem sản phẩm]
Guest --> SearchProducts[Tìm kiếm sản phẩm]
Guest --> FilterProducts[Lọc sản phẩm]
Guest --> Register[Đăng ký tài khoản]

%% USER
User --> ViewProducts
User --> ViewDetail[Xem chi tiết sản phẩm]
User --> AddToCart[Thêm vào giỏ hàng]
User --> ApplyVoucher[Áp mã giảm giá]
User --> Checkout[Thanh toán]
User --> ViewOrders[Xem đơn hàng]

%% MANAGER
Manager --> ManageProducts[Quản lý sản phẩm]
Manager --> ManageInventory[Quản lý tồn kho]
Manager --> ManageOrders[Quản lý đơn hàng]
Manager --> ViewRevenue[Xem doanh thu]

%% ADMIN
Admin --> ManageUsers[Quản lý tài khoản]
Admin --> ManageProducts

%% RELATIONSHIPS
Checkout --> InputInfo[Nhập thông tin giao hàng]
Checkout --> Payment[Xử lý thanh toán]

ApplyVoucher -.-> Checkout
